---
import TypingAnimation from './TypingAnimation.astro';

const highlights = [
  '인하대학교 컴퓨터공학과 졸업',
  'IT 동호회 SOPT (Android 파트장), Depromeet',
  'HECAS → Querensys 선임 연구원 (7년 6개월)'
];

const skills = [
  { name: 'Android', icon: '🤖' },
  { name: 'Kotlin', icon: '🎯' },
  { name: 'Web', icon: '🌐' },
  { name: 'React', icon: '⚛️' }
];
---

<section id="about" class="min-h-screen flex items-center">
  <div class="max-w-3xl mx-auto px-6 md:px-8 py-16 md:py-24 w-full">
    <div class="flex flex-col items-center text-center space-y-8">
      {/* Status Badge */}
      <div class="inline-flex items-center gap-2 px-10 py-3 border border-border-fix rounded-half text-sm text-secondary-foreground">
        <span class="w-2 h-2 rounded-full bg-green-500 animate-pulse"></span>
        구직중
      </div>

      {/* Main Greeting */}
      <h1 class="text-5xl md:text-6xl lg:text-7xl tracking-tight text-foreground" style={{ fontFamily: 'var(--font-heading)' }}>
        Hello, I'm<br>Dongsoek Lee
      </h1>


      {/* Role - designed for typing animation */}
      <p class="text-xl md:text-2xl lg:text-3xl text-primary flex items-center gap-2">
        <span class="inline-block">&gt;</span>
        <TypingAnimation text="Application Developer" speed={80} className="text-xl font-bold" />
      </p>

      {/* Career History */}
      <div class="space-y-3 pt-4">
        {highlights.map((highlight, index) => (
          <p class="text-sm md:text-base text-muted-foreground leading-relaxed">
            {highlight}
          </p>
        ))}
      </div>

      {/* Skill Badges */}
      <div class="flex flex-wrap gap-3 pt-4 justify-center">
        {skills.map((skill) => (
          <span 
            class="inline-flex items-center gap-2 px-4 py-2 bg-secondary border border-border text-sm text-secondary-foreground hover:bg-primary hover:text-primary-foreground transition-colors"
          >
            <span class="text-base">{skill.icon}</span>
            {skill.name}
          </span>
        ))}
      </div>
    </div>
  </div>
</section>